# NOTE: tests below require a running PostgreSQL test database and are
# structured with supertest against the Express app. Set DATABASE_URL to a
# disposable test database and run `npm run migrate` before executing.

